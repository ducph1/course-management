<div class="mt-4">
    <div class="row">
        <div class="col-lg-3 left-sidebar">
            <button type="button" class="btn btn-primary">Học ngay</button>
            <ul>
                <li>{{course.level}}</li>
            </ul>
        </div>
        <div class="col-lg-9 right-sidebar">
            <h2>{{course.name}}</h2>
            <iframe width="853" height="480" src="https://www.youtube.com/embed/{{course.videoId}}" frameborder="0" allow="accelerometer; autoplay; clipboard-write; encrypted-media; gyroscope; picture-in-picture" allowfullscreen></iframe>
            <p>{{course.description}}</p>
        </div>
    </div>
</div>